{% extends "kulupulang/base/base.jinja" %}

{% block content %}
    <h2>submit batch</h2>
    <p>this will put batch #{{ batch.pk }} ({{ batch.name }}) to the oven.</p>
    <p>once in the oven, you won't be able to make further changes to the batch so be sure before you click!</p>
    <p>in the oven, anyone can raise a dispute. if it finishes cooking ({{ batch.voting_hours }} hours) without any disputes, its words and roots will take pride of place in the dictionary.</p>
    <form action="" method="POST">
        {% csrf_token %}
        <div class="actions">
            <button type="submit">submit it!</button>
        </div>
    </form>
{% endblock content %}